interface:
  display_name: "Tracework Capture"
  short_description: "Capture one session or recover a scoped day"
  default_prompt: "Use $capture to record this session with dynamic depth, or use Capture Day to recover scoped report-ready evidence from today's indexed sessions."
