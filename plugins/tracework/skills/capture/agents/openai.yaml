interface:
  display_name: "Tracework Capture"
  short_description: "Capture report-ready session memory"
  default_prompt: "Use $capture to record report-ready session memory with evidence boundaries and artifact dossier context."
